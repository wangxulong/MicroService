<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:daemon="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="5dp"
    android:paddingRight="5dp"
    >
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:gravity="left|center_vertical"
            android:textSize="18sp"
            android:textColor="@color/colorMain"
            android:text="@string/hot_question" />
        <ListView
            android:id="@+id/question_hot"
            android:layout_width="match_parent"
            android:layout_height="240dp"></ListView>
        <TextView
            android:layout_width="match_parent"
            android:layout_height="28dp"
            android:gravity="left|center_vertical"
            android:textSize="18sp"
            android:textColor="@color/colorMain"
            android:text="@string/later_update" />
        <ListView
            android:id="@+id/question_later_update"
            android:layout_width="match_parent"
            android:layout_height="800dp"></ListView>
    </LinearLayout>

</ScrollView>
